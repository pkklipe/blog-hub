const botoes = document.querySelecrtorAll("button");

botoes.forEach(botao => {
    botao,addEventventilistener("click", )

    function curtir() {
        let curtida = document.querySelecrtor("span")
        curtida.textContent++
    }
});